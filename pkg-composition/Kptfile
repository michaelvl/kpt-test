apiVersion: kpt.dev/v1
kind: Kptfile
metadata:
  name: composed-pkg
  annotations:
    config.kubernetes.io/local-config: "true"
info:
  description: A composed package
pipeline:
  mutators:
    - name: set labels
      image: gcr.io/kpt-fn/set-annotations:v0.1.4
      configMap:
        parentLabel: SetByParent
  validators:
    - image: gcr.io/kpt-fn/kubeval:v0.3
      name: validation
